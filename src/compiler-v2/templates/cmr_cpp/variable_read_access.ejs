<%
function formatAccessor(accessors)
{
	var res = "";
	for (var i in accessors)
		res += "[" + accessors[i].toLatexString() + "]";
	return res;
}
%>
<%- object.longName %><%- formatAccessor(object.getUsageOps(context,latexEntity)) %>
