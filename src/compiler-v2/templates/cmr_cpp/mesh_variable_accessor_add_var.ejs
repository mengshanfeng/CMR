<%
function extraDefs(type,defs)
{
	var ret = type;
	for (var i in defs)
		ret += "["+defs[i].size+"]";
	return ret;
}
%>//define variable <%- object.latexName %>
	this->addVariable("<%- object.longName %>",sizeof( <%- extraDefs(object.type,object.defs) %> ), <%- object.ghostCnt %> );
